import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    app: {
        height: '100vh'
    },
    homeWrapper: {
        background: '#fff'
    }
}));
